name: Docs Linting

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  docs-linting:
    uses: ROCm/rocm-docs-core/.github/workflows/linting.yml@develops
